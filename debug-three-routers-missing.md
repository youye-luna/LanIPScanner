# Debug Session: three-routers-missing

- Status: [OPEN]
- Symptom: 同一扫描范围内有三台路由器，但程序只识别到一台。
- Expected: 三台路由器均被发现并标记为 DHCP服务器/路由器候选。

## Hypotheses

1. 只有默认网关会被直接标记，另外两台路由器因主机名无明确关键词而未标记。
2. Ping 成功的非默认网关不会执行管理端口探测，导致路由器证据为空。
3. Ping 失败时“首个端口成功即取消”可能先命中普通端口，错过管理端口或 TCP 53。
4. 另外两台路由器位于扫描范围内且在线，但 DNS/主机名解析结果不足以触发当前规则。
5. 多网卡环境下默认网关选择与当前扫描网段不匹配。

## Evidence Plan

- 记录每个在线 IP 的 Ping 结果、开放端口、主机名、选中的默认网关及最终 DHCP 判定原因。
- 复现一次包含三台路由器的扫描并对比三台目标 IP 的日志。

## Progress

- [x] 建立调试会话
- [ ] 添加仅用于观测的日志插桩
- [ ] 收集复现日志
- [ ] 根据证据确定根因
- [ ] 最小修复
- [ ] 修复后验证
- [ ] 用户确认与清理
